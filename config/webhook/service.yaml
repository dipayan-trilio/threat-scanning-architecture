apiVersion: v1
kind: Service
metadata:
  name: threat-scanning-webhook-service
  namespace: threat-scanning-system
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app.kubernetes.io/name: threat-scanning-controller
    app.kubernetes.io/component: webhook
