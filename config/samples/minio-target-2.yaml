apiVersion: threatscanning.trilio.io/v1
kind: Target
metadata:
  name: minio-target-2
spec:
  targetType: TVK
  type: ObjectStore
  vendor: Other
  objectStoreCredentials:
    url: http://192.168.0.113:9000
    region: us-east-1
    bucketName: fuse
    credentialSecret:
      name: minio-local-secret
      namespace: default
    skipCertVerification: true
  thresholdCapacity: 100Gi

