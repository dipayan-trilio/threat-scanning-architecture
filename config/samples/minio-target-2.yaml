apiVersion: threatscanning.trilio.io/v1
kind: Target
metadata:
  name: minio-target-2
spec:
  type: ObjectStore
  vendor: Other
  objectStoreCredentials:
    url: https://api.minio.k8strilio.net
    region: us-east-1
    bucketName: dipayan-ts
    credentialSecret:
      name: minio-target-secret
      namespace: default
    skipCertVerification: true
  thresholdCapacity: 100Gi

