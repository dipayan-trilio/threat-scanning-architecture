"""
Prescan package for threat scanning service.

Validates backup targets and paths, detects backup types, checks for VM workloads,
and updates ScanInstance CRs with appropriate labels and annotations.
"""

__version__ = '1.0.0'

